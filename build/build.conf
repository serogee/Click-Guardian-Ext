# Build Configuration
VERSION=1.0.0
COMPANY_NAME=Click Guardian Ext Project
PRODUCT_NAME=Click Guardian Ext
DESCRIPTION=Prevents accidental double-clicks with configurable delay protection
COPYRIGHT=(c) 2026 Azizul Hakim

# Build settings
BUILD_OUTPUT_DIR=dist
BUILD_TEMP_DIR=build/temp

# Code signing uses the SIGN_CERT_FILE, SIGN_CERT_PASSWORD, and
# SIGN_TIMESTAMP_URL environment variables. Do not store credentials here.

# Advanced build options
ENABLE_UPX_COMPRESSION=false
ENABLE_DEBUG_SYMBOLS=false
ENABLE_VERBOSE_BUILD=false
